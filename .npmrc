# 1. 冻结所有非白名单的生命周期脚本，防范投毒脚本激活
ignore-scripts=true

# 2. 强行锁定 lockfile，禁止任何静默的依赖树变更
frozen-lockfile=true

# 3. 每次安装强制校验本地 store 的内容完整性，严防本地缓存被篡改
verify-store-integrity=true

# 4. 确保不将依赖提升，利用 pnpm 的严格软链机制隔离幽灵依赖
shamefully-hoist=false

# 5. 允许在终端中展示更清晰的依赖树构建警告
strict-peer-dependencies=true
